'use script';
// Test main JS file.
console.log('Your main JS file is working fine.');

// Change the second heading from a first level heading to a second level heading.
document.getElementById('beginning').outerHTML = "<h2>It's Only The Beginning!</h2>";
